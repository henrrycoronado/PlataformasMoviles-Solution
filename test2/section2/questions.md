# Sección II: Práctica (60 puntos)

## 5. Enhanced Enums (20 pts)

Crea un `Enum` llamado `EstadoPedido` con tres valores: `pendiente`, `enviado`, `entregado`. Haz que el enum tenga un método llamado `descripcion()` que retorne un `String` diferente para cada estado (ej: "Su pedido está en camino").

## 6. Navegación y Datos (20 pts)

Escribe el fragmento de código necesario para navegar a una ruta llamada `'/perfil'` pasando como argumento un objeto de una clase `Usuario` que contenga `nombre` y `edad`.

## 7. Composición de Widgets (20 pts)

Crea un `StatelessWidget` que represente una tarjeta de producto. Debe tener:
