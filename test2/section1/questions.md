# Sección I: Teoría (40 puntos)

## 1. Selección Simple (10 pts)

Si necesitas navegar a una nueva pantalla y eliminar la pantalla actual del historial para que el usuario no pueda regresar (ej. después de un Login), ¿qué método de `Navigator` usarías?

- [ ] a) `Navigator.push()`
- [ ] b) `Navigator.pushReplacement()`
- [ ] c) `Navigator.pop()`
- [ ] d) `Navigator.of(context).maybePop()`

---

## 2. Verdadero o Falso (10 pts)

- Una variable de tipo `num` en Dart puede almacenar tanto valores de tipo `int` como de tipo `double`.
  - [ ] Verdadero
  - [ ] Falso

- El widget `Scaffold` es obligatorio para que cualquier aplicación de Flutter pueda compilar sin errores de renderizado.
  - [ ] Verdadero
  - [ ] Falso

---

## 3. Rellenar Espacios (10 pts)

En una `Row`, la propiedad que alinea a los hijos en el eje horizontal se llama ________________________, mientras que en una `Column`, esa misma propiedad los alinea en el eje vertical.

---

## 4. Lógica de Tipos (10 pts)

¿Qué sucede si intentas asignar un valor `double` a una variable declarada como `int` en Dart? Justifica tu respuesta brevemente basándote en el sistema de tipos del lenguaje.